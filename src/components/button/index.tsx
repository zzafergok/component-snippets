import DefaultButton from "./default_button";

function ButtonIndex() {
  return (
    <div>
      <DefaultButton onClick={() => alert("I am work!!!")} />
    </div>
  );
}

export default ButtonIndex;
